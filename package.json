{
  "name": "geox",
  "version": "1.0.1",
  "description": "a monorepo for geospatial typescript-to-XML libraries",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo \"Error: no build specified\" && exit 1",
    "lint": "echo \"Error: no lint specified\" && exit 1",
    "test": "echo \"Error: no test specified\" && exit 0"
  },
  "keywords": [],
  "author": "Steven Kalt <support+geo-xml@kalt.cloud>",
  "devDependencies": {
    "prettier": "^3.3.3",
    "typescript": "~5.5.2",
    "vite": "^5.0.0",
    "vitest": "^1.3.1",
    "cspell": "^8.15.1"
  }
}
